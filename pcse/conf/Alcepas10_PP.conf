# -*- coding: utf-8 -*-
# 版权所有 (c) 2004-2024 Wageningen Environmental Research, Wageningen-UR
# Allard de Wit (allard.dewit@wur.nl), 2024年3月
"""PCSE 为 ALCEPAS 洋葱模型的潜在生产力模拟的配置文件
"""

from pcse.soil.classic_waterbalance import WaterbalancePP
from pcse.crop.alcepas import ALCEPAS
from pcse.agromanager import AgroManager

# 用于水分平衡的模块
SOIL = None

# 用于作物模拟本身的模块
CROP = ALCEPAS

# 用于农事管理操作的模块
AGROMANAGEMENT = AgroManager

# 在OUTPUT信号时要保存的变量
# 如果不需要任何OUTPUT，请设置为空列表
OUTPUT_VARS = ["DVS","BULBSUM", "BULB", "LAI", "TADRW", "WSO", "GLA", "TSUMEM"]
# OUTPUT信号的间隔，可以是"daily"|"dekadal"|"monthly"|"weekly"
# 如果是每日输出，可以通过OUTPUT_INTERVAL_DAYS更改连续输出的天数
# 如果是十日或月度输出，则忽略此项
OUTPUT_INTERVAL = "daily"
OUTPUT_INTERVAL_DAYS = 1
# 周几：星期一为0，星期日为6
OUTPUT_WEEKDAY = 0

# 在CROP_FINISH信号时要保存的汇总变量
# 如果不需要任何SUMMARY_OUTPUT，请设置为空列表
SUMMARY_OUTPUT_VARS = ["DOS", "DOE", "DOB50", "DOF50", "TADWR", "WSO", "WRT", "WLV"]

# 在TERMINATE信号时要保存的汇总变量
# 如果不需要任何TERMINAL_OUTPUT，请设置为空列表
TERMINAL_OUTPUT_VARS = []
