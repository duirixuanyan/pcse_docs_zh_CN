# -*- coding: utf-8 -*-
# Copyright (c) 2004-2024 Wageningen Environmental Research
# Allard de Wit (allard.dewit@wur.nl), February 2024
"""PCSE配置文件，适用于WOFOST 8.1 潜力生产力
"""

from pcse.soil.soil_wrappers import SoilModuleWrapper_PP
from pcse.crop.wofost81 import Wofost81
from pcse.agromanager import AgroManager

# 用于水分平衡的模块
SOIL = SoilModuleWrapper_PP

# 用于作物模拟的模块
CROP = Wofost81

# 用于农事管理动作的模块
AGROMANAGEMENT = AgroManager

# 在OUTPUT信号时要保存的变量
# 如果不需要任何OUTPUT，则设为空列表
OUTPUT_VARS = ["DVS","LAI", "TAGP", "WSO", "WLV", "WST", "TSUM", "TSUME",
               "WRT", "TRA", "EVS", "RD", "SM", "NamountSO", "NamountLV",
               "NamountST", "NamountRT", "NuptakeTotal"]
# OUTPUT信号的输出间隔，可以为 "daily"|"dekadal"|"monthly"
# 若为"daily"，可通过OUTPUT_INTERVAL_DAYS设置连续输出之间的天数。
# 若为"dekadal"或"monthly"，此设置被忽略。
OUTPUT_INTERVAL = "daily"
OUTPUT_INTERVAL_DAYS = 1
# 星期：周一为0，周日为6
OUTPUT_WEEKDAY = 0

# 在CROP_FINISH信号下保存的汇总变量
# 如果不需要任何SUMMARY_OUTPUT，则设为空列表
SUMMARY_OUTPUT_VARS = ["DVS","LAIMAX","TAGP", "TWSO", "TWLV", "TWST",
                       "TWRT", "CTRAT", "RD", "DOS", "DOE", "DOA",
                       "DOM", "DOH", "DOV", "CEVST",
                       "NuptakeTotal", "NamountSO"]

# 在TERMINATE信号下保存的汇总变量
# 如果不需要任何TERMINAL_OUTPUT，则设为空列表
TERMINAL_OUTPUT_VARS = ["WTRAT", "EVST", "EVWT", "TSR", "RAINT", "TOTINF",
                        "TOTIRR", "PERCT", "LOSST"]
