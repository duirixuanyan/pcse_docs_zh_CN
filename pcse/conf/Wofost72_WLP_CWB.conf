# -*- coding: utf-8 -*-
# 版权所有 (c) 2004-2021 Wageningen Environmental Research
# Allard de Wit (allard.dewit@wur.nl)，2021年8月
"""PCSE 配置文件，用于 WOFOST 7.2 水分受限产量模拟

此配置文件定义了用于自由排水土壤的水分受限产量模拟中
所需使用的土壤和作物组件。
"""

from pcse.soil.classic_waterbalance import WaterbalanceFD
from pcse.crop.wofost72 import Wofost72
from pcse.agromanager import AgroManager

# 用于水分平衡计算的模块
SOIL = WaterbalanceFD

# 用于作物模拟本身的模块
CROP = Wofost72

# 用于农事管理动作的模块
AGROMANAGEMENT = AgroManager

# 在OUTPUT信号处需要保存的变量
# 如不需要任何输出，将其设为空列表
OUTPUT_VARS = ["DVS","LAI","TAGP", "TWSO", "TWLV", "TWST",
               "TWRT", "TRA", "RD", "SM", "WWLOW", "RFTRA"]
# OUTPUT 信号的输出间隔，可选 "daily"|"dekadal"|"monthly"|"weekly"
# 如果是每日输出，可以通过 OUTPUT_INTERVAL_DAYS 更改输出间隔天数。
# 对于旬、月输出则此选项无效。
OUTPUT_INTERVAL = "daily"
OUTPUT_INTERVAL_DAYS = 1
# 星期几：星期一是0，星期天是6
OUTPUT_WEEKDAY = 0

# 在CROP_FINISH信号处需要保存的汇总变量
# 如不需要任何汇总输出，将其设为空列表
SUMMARY_OUTPUT_VARS = ["DVS","LAIMAX","TAGP", "TWSO", "TWLV", "TWST",
                       "TWRT", "CTRAT", "RD", "DOS", "DOE", "DOA",
                       "DOM", "DOH", "DOV", "CEVST"]

# 在TERMINATE信号处需要保存的汇总变量
# 如不需要任何终端输出，将其设为空列表
TERMINAL_OUTPUT_VARS = ["WTRAT", "EVST", "EVWT", "TSR", "RAINT", "TOTINF",
                        "TOTIRR", "PERCT", "LOSST"]