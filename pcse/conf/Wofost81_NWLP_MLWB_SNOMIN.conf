# -*- coding: utf-8 -*-
# 版权所有 (c) 2004-2024 Wageningen Environmental Research
# Herman Berghuijs (herman.berghuijs@wur.nl) 和 Allard de Wit (allard.dewit@wur.nl), 2024年1月
"""PCSE 配置文件，用于 WOFOST 8.1 水分和氮素限制条件下的生产模拟，使用
多层土壤水分平衡和 SNOMIN 土壤碳/氮平衡。
"""

from pcse.soil.soil_wrappers import SoilModuleWrapper_NWLP_MLWB_SNOMIN
from pcse.crop.wofost81 import Wofost81
from pcse.agromanager import AgroManager

# 用于水分平衡的模块
SOIL = SoilModuleWrapper_NWLP_MLWB_SNOMIN

# 用于作物本身模拟的模块
CROP = Wofost81

# 用于农事管理操作的模块
AGROMANAGEMENT = AgroManager

# 在 OUTPUT 信号中要保存的变量
# 如果不需要任何输出，请设为空列表
OUTPUT_VARS = ["DVS", "LAI", "TAGP", "WSO", "WLV", "WST",
               "WRT", "RD", "SM", "RFTRA", "RMINT", "RIRR", "EVS", "TRA", "CEVST", "CTRAT",
                "NAVAIL", "Ndemand", "NamountSO", "NamountLV", "NamountST", "NamountRT",
                "SM", "WC", "NH4", "NO3","NLOSSCUM", "NuptakeTotal"]
# OUTPUT 信号的时间间隔，可选 "daily"|"dekadal"|"monthly"
# 对于每日输出，可以使用 OUTPUT_INTERVAL_DAYS 修改两次输出之间的天数。
# 对于旬或月输出，该参数被忽略。
OUTPUT_INTERVAL = "daily"
OUTPUT_INTERVAL_DAYS = 1
# 星期：星期一为0，星期日为6
OUTPUT_WEEKDAY = 0

# 在 CROP_FINISH 信号处保存的汇总变量
# 如果不需要任何 SUMMARY_OUTPUT，请设为空列表
SUMMARY_OUTPUT_VARS = ["DVS","LAIMAX","TAGP", "TWSO", "TWLV", "TWST",
                       "TWRT", "CTRAT", "RD", "DOS", "DOE", "DOA",
                       "DOM", "DOH", "DOV", "CEVST", "NuptakeTotal",
                       "NamountSO"]

# 在 TERMINATE 信号处保存的汇总变量
# 如果不需要任何 TERMINAL_OUTPUT，请设为空列表
TERMINAL_OUTPUT_VARS = ["WTRAT", "EVST", "EVWT", "TSR", "RAINT", "TOTINF",
                        "TOTIRR", "PERCT", "LOSST"]
